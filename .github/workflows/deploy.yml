name: Deploy para Hostinger
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 212.85.9.20
          username: u863365274
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 65002
          script: cd /home/u863365274/domains/opennpsi.com/public_html && git pull origin main
